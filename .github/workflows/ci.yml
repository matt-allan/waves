name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install Python dependencies
        run: uv sync

      - name: Cache tools
        uses: actions/cache@v4
        with:
          path: tools
          key: tools-${{ hashFiles('scripts/install_tools.py') }}

      - name: Install tools
        run: uv run python scripts/install_tools.py

      - name: Configure
        run: uv run python configure.py

      - name: Build ROM
        run: ninja

      - name: Build libsameboy
        run: ninja build/lib/libsameboy.so

      - name: Run tests
        run: uv run pytest
